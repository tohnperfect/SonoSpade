"""Vendored, self-contained copies of the SE(3) geometry (se3), the cross-repo contract
constants/classes (contracts), and the B-mode scan geometry (us_render), originally from the
authors' sibling repositories (probe_action_recognizer `par`). Copied here so this SonoSPADE
release runs standalone with no external repo on the path. Pure numpy + stdlib."""
